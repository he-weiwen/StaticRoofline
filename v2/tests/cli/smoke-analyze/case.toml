# Smoke test for positional-argument parsing and the {case} placeholder.
# The stub never opens the file, so in.ptx here is a placeholder; PR 12
# turns this into a real analyze test with expected.json.
args = ["analyze", "{case}/in.ptx"]
exit = 2
check_stream = "stderr"

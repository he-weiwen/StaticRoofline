# S1 (PLAN.md §4): is this kernel's design point what I computed on
# paper? k5 at BM=64 BN=64 BK=8 TM=8 TN=8. Flips at PR 13 (verdicts).
# AI(global) is shape-independent, so the verdict pair needs no --bind.
args = ["analyze", "{fixtures}/k5/k5.sm_80.ptx", "--json",
        "--arch", "sm_80", "--arch", "sm_86"]
json = true

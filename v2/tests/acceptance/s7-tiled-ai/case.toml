# S7.2 (PLAN.md §4): did tiling pay off? — the tiled side. k5's outer
# tile loop runs at AI(global) = 32 flop/B, shape-independent. The
# 64x contrast with S7.1's 0.5 is the answer. Flips at PR 12.
args = ["analyze", "{fixtures}/k5/k5.sm_80.ptx", "--json"]
json = true

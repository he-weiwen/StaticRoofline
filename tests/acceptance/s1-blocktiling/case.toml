# S1 (PLAN.md §4): is this kernel's design point what I computed on
# paper? k5 at BM=64 BN=64 BK=8 TM=8 TN=8. Flips at PR 13.
# AI(global) is shape-independent, so the machine pair needs no --bind:
# 32 flop/B sits above the A100's f32 peak ratio (12.5 flop/B) and
# below the RTX 3090's (38.0) — the reader's comparison, printed with both numbers.
args = ["analyze", "{fixtures}/k5/k5.sm_80.ptx", "--json",
        "--arch", "sm_80", "--arch", "sm_86"]
json = true
